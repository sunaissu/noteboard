# Noteboard playground

A Vite application that imports `@sunaissu/noteboard` directly from `../src` for fast local package development.

```bash
npm install
npm run dev
```

The playground covers configurable toolbar and properties-panel placement, custom themes, host-controlled read-only mode, imperative viewport presets, exports, and live viewport reporting.
